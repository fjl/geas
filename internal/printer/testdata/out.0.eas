;;; This document has an assortment of statements to test the printer.

#define foo(a) = $a + 2

#define %macro {
    push 1234 + a      ; comment 1
    push 0             ; comment 2
    revert
}

    push 0
    push1 a + b*c - foo(2)
    add
    %macro2            ; note this will lose the parentheses

label:
    jump @label

;;;; The end.
.dotted:
    push @.dotted

#include "foo.eas"
#assemble "otherfile.eas"

#pragma target "yolo"
